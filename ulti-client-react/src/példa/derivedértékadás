public static getDerivedStateFromProps<T, Key>(props: Readonly<IDataGridProps<T, Key>>, state: ITableState): ITableState {
        if (props.sortKey || props.sortOrder) {
            return {
                ...state,
                sortKey: props.sortKey ?? state.sortKey,
                sortOrder: props.sortOrder ?? state.sortOrder,
            };
        }
        return null;
    }


    csak azért, hogy ha sok adattagja lenne a state-ednek, akkor nem kell mindig mindnek értéket adni, a ... operátorral mondhatod azt, hogy ugyanaz mint a korábbi aztán felsorolod hogy mi legyen más



     ...this.graphOptions,
                        xaxis: {
                            ...this.graphOptions.xaxis,
                            min: this.graphXMin,
                            max: this.graphXMax
                        }
                    };